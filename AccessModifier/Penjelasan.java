package AccessModifier;

public class Penjelasan {
  // public -> bisa diakses dari class, package (yg sama), subclass, world
  // protected -> bisa diakses dari class, package (yg sama), subclass
  // private -> bisa diakses dari class yg sama saja
  // default (no-modifier) -> bisa diakses dari class, package (yg sama)
}
